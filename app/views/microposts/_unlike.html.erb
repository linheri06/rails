<%= form_for(@micropost.active_micropost_likes.find_by(user_like_id: current_user.id),
            html: { method: :delete }) do |f| %>
    <div><%= hidden_field_tag :micropost_id, @micropost.id %></div>
    <%= button_tag(type: 'submit', class: 'btn btn-primary', id: 'like_button_602') do %>
        <i class="bi bi-hand-thumbs-up"></i>
    <% end %>
<% end %>